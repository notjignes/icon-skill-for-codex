# Changelog

## 0.2.0-rc.1 — Unreleased

- Replaced duplicate base, material, and character PNG trees with contained manifest aliases to 72 unchanged high-resolution archive masters.
- Added a SHA-locked canonical resolver, 73-service registry, 70 base mappings, 72 material mappings, and 11 character aliases.
- Added deterministic identity-gated planning, request-scoped reference precedence, role-distinct approved reference retrieval, and abstention on incompatible subjects.
- Added native high-resolution master preservation plus `1024x768` delivery export from an explicit source path.
- Added owner-gated candidate staging, visual review, held-out transfer evidence, quarantine, and promotion tooling.
- Added the silent runtime contract and one Image Gen progress line.
- Added compact-package validation, regression tests, CI, and an atomic rollback-capable installer.
- Hardened natural edit clauses, repeated identities, purifier variants, character/service
  aliases, and resumable ambiguity choices against false base selection.
- Hardened transfer evidence with decimal score boundaries, strict shuffled-order
  uniqueness, explicit failure flags, normalized reviewers, shared generation-policy
  fingerprints, and path-free installed records.
- Made the quirky generation wait message flexible tone guidance rather than fixed copy;
  runtime still emits only one short waiting line and keeps exact exports silent.
- Prevented object identities such as `impact driver` from triggering character routing,
  blocked automatic references for identity-unsafe compounds, and required request-scoped
  references to decode as images.
- Rejected symlinked resolver paths and added runtime-bank orphan and content-version
  consistency checks.
- Derived transfer means from complete per-run blind ratings and preserved those ratings
  in the path-free installed evidence record.

No new reusable image reference is included in this release candidate without a separate explicit owner approval.

## Initial repository groundwork

- Added a README example gallery for representative UC skeuomorphic use-case results.
- Added the unpacked `uc-icons` Codex skill as the repository source of truth.
- Bundled service archive PNGs, character bases, manifests, and lookup/export scripts.
- Used manifest `image_path` aliases for base-object and material-index meanings.
- Added fixed `1024x768` export behavior with UC Grey (Grey 10) validation.
- Clarified that the skill is Codex-only and uses GPT Image 2 / Image Gen through the companion `imagegen` skill.
